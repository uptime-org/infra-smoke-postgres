# Infra tier base template configuration
# These sync patterns define which files are Galaxy-managed vs user-owned

sync_patterns:
  galaxy_infrastructure:
    - "sidecar/*"
    - "init/*"
    - "Dockerfile"
    - "build.sh"
    - "helm/*"
    - ".galaxy/*"
    - ".github/workflows/deploy.yml"
  user_owned:
    - "README.md"
    - ".gitignore"